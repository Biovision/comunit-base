<% if collection.any? %>
    <ul class="list-of-entities">
      <% collection.each do |entity| %>
          <% if entity.deleted? %>
              <li class="deleted">
                <%= t(:deleted_entity) %>, id: <%= entity.id %>
                (<%= entity.screen_name %>)
              </li>
          <% else %>
              <li data-url="<%= user_admin_group_path(id: group.id, user_id: entity.id) %>">
                <%= render partial: 'admin/groups/users/user', locals: { entity: entity } %>
              </li>
          <% end %>
      <% end %>
    </ul>
<% else %>
    <%= render 'shared/nothing_found' %>
<% end %>
