<div class="toggleable" data-url="<%= toggle_admin_event_material_path(entity) %>">
  <% EventMaterial.toggleable_attributes.each do |flag| %>
  <span class="<%= entity.attributes[flag.to_s] ? 'active' : 'inactive' %>" data-flag="<%= flag %>">
    <%= t("activerecord.attributes.event_material.#{flag}") %>
  </span>
  <% end %>
</div>
