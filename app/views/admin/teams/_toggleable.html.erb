<div class="toggleable" data-url="<%= toggle_admin_team_path(entity) %>">
  <% Team.toggleable_attributes.each do |flag| %>
  <span class="<%= entity.attributes[flag.to_s] ? 'active' : 'inactive' %>" data-flag="<%= flag %>">
    <%= t("activerecord.attributes.team.#{flag}") %>
  </span>
  <% end %>
</div>
