<% content_for :meta_title, t('.title', category: @entity.name, page: current_page) %>
<% content_for :hide_sidebar, '1' %>

<article class="entity-page">
  <h1><%= @entity.name %></h1>
  <ul class="actions">
    <li><%= back_icon admin_news_category_path(@entity), t('activerecord.models.news_category') %></li>
    <li><%= list_icon admin_news_categories_path %></li>
  </ul>

  <section>
    <h2><%= t('.heading') %></h2>

    <%= paginate @collection %>
    <%= render partial: 'admin/news/list', locals: { collection: @collection } %>
    <%= paginate @collection %>
  </section>
</article>