<div class="toggleable" data-url="<%= toggle_admin_news_path(entity) %>">
  <% News.toggleable_attributes.each do |flag| %>
  <span class="<%= entity.attributes[flag.to_s] ? 'active' : 'inactive' %>" data-flag="<%= flag %>">
    <%= t("activerecord.attributes.news.#{flag}") %>
  </span>
  <% end %>
</div>
