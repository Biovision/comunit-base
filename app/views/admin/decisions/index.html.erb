<% content_for :meta_title, t('.title') %>
<% content_for :breadcrumbs do %>
  <span><%= t('admin.decisions.nav_item.text') %></span>
<% end %>

<article>
  <h1><%= t('.heading') %></h1>
  <ul class="actions">
    <li><%= create_icon(new_admin_decision_path) %></li>
  </ul>

  <%=
    render(
        partial: 'shared/admin/list',
        locals: {
            collection: @collection
        }
    )
  %>
</article>
