<div class="toggleable" data-url="<%= toggle_admin_event_participant_path(id: entity.id) %>">
  <% EventParticipant.toggleable_attributes.each do |flag| %>
  <span class="<%= entity.attributes[flag.to_s] ? 'active' : 'inactive' %>" data-flag="<%= flag %>">
    <%= t("activerecord.attributes.event_participant.#{flag}") %>
  </span>
  <% end %>
</div>
