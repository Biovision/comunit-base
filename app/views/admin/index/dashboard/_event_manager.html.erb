<% if current_user_has_privilege?(:event_manager) %>
    <nav>
      <div class="heading"><%= t('.heading') %></div>
      <ul>
        <li><%= render 'admin/events/nav_item' %></li>
      </ul>
    </nav>
<% end %>
