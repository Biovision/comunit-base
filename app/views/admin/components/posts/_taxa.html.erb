<% if collection.any? %>
  <section>
    <h2><%= t('.heading') %></h2>

    <%=
      render(
          partial: 'shared/admin/list_with_priority',
          locals: { collection: collection }
      )
    %>
  </section>
<% end %>
