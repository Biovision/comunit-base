<div class="follow-user-buttons" data-url="<%= follow_api_user_path(user.id) %>">
  <%= button_tag t(:follow), class: "button-save follow #{followee ? 'hidden' : ''}" %>
  <%= button_tag t(:unfollow), class: "button-destroy unfollow #{followee ? '' : 'hidden'}" %>
</div>
