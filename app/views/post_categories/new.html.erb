<% content_for :meta_title, t('.title') %>
<% content_for :breadcrumbs do %>
    <%= link_to(t('admin.post_categories.nav_item.text'), admin_post_categories_path) %>
    <span><%= t('.heading') %></span>
<% end %>

<article>
  <h1><%= t('.heading') %></h1>
  <ul class="actions">
    <li><%= back_icon(admin_post_categories_path) %></li>
  </ul>

  <%= render partial: 'form', locals: { entity: @entity } %>
</article>
