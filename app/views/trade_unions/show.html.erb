<% content_for :meta_title, t('.title', name: @entity.name) %>
<% content_for :breadcrumbs do %>
  <%= link_to(t('trade_unions.index.nav_text'), trade_unions_path) %>
  <span><%= @entity.name %></span>
<% end %>

<article class="trade-union">
  <div class="content-wrapper">
    <h1><%= @entity.name %></h1>

    <div class="text"><%= raw(@entity.description) %></div>
  </div>
</article>