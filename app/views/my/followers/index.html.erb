<% content_for :meta_title, t('.title', page: current_page) %>
<% content_for :selected_item, 'my-followers' %>
<% content_for :secondary_navigation, '1' %>

<article>
  <h1><%= t('.heading') %></h1>

  <ul class="actions">
    <li><%= back_icon(my_path) %></li>
    <li><%= return_icon(users_path, t('users.index.heading')) %></li>
  </ul>

  <%= render partial: 'users/filter', locals: { filter: @filter } %>

  <%= paginate @collection %>
  <%= render partial: 'list', locals: { collection: @collection } %>
  <%= paginate @collection %>
</article>
