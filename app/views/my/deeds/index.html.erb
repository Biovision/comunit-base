<% content_for :meta_title, t('.title', page: current_page) %>

<article class="profile-content">
  <h1><%= t('.heading') %></h1>

  <nav>
    <%= link_to('Добавить', new_deed_path, class: 'button-nav') %>
  </nav>

  <%= paginate @collection %>
  <%= render partial: 'list', locals: { collection: @collection } %>
  <%= paginate @collection %>
</article>
