<% content_for :meta_title, t('.title', user: @entity.profile_name, page: current_page) %>

<article>
  <div class="content-wrapper">
    <h1><%= t('.heading', user: @entity.profile_name) %></h1>

    <%= paginate @collection %>
    <%= render partial: 'posts/list', locals: { collection: @collection } %>
    <%= paginate @collection %>
  </div>
</article>
