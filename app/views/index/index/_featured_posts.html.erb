<% if collection.load.any? %>
  <section class="posts-featured">
    <h2>Тема дня</h2>

    <div class="js-biovision-carousel" data-timeout="5000">
      <button class="prev" type="button"></button>
      <ul class="carousel-container">
        <% collection.each do |post| %>
          <li class="carousel-item">
            <div class="posts-featured-in-carousel">
              <figure>
                <%=
                  post_link(
                      post,
                      simple_image_medium(post),
                      class: 'proportional-container r-3x2'
                  )
                %>
              </figure>
              <div class="text">
                <div class="title"><%= post_link(post) %></div>
                <%= time_tag(post.publication_time) %>
                <p><%= post_link(post, post.lead) %></p>
              </div>
              <div class="footer">
                <div class="post-counters">
                  <span class="comments"><%= post.comments_count %></span>
                  <span class="views"><%= post.view_count %></span>
                </div>
              </div>
            </div>
          </li>
        <% end %>
      </ul>
      <button class="next" type="button"></button>
    </div>
  </section>
<% end %>
