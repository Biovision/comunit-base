<div class="frontpage-posts">
  <div class="heading">
    Новости регионов
  </div>
  <div class="posts">
    <%= render partial: 'posts/list', locals: { collection: posts } %>
    <div class="load-more" data-url="<%= index_regional_news_path(page: 2, region_slug: current_region.long_slug, region_id: param_from_request(:redion_id)) %>">
      <%= button_tag 'Загрузить ещё', type: :button, class: 'button-action' %>
    </div>
  </div>
</div>
